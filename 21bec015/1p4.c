// program to find the value of y
#include <stdio.h>
int main(){
    float x , y;
    printf("enter the value of x ");
    scanf("%f",&x);
    y=(5*x + 3);
    printf("the value of y is %f",y);

    return 0;
}